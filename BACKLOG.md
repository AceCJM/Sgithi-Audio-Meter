# Backlog

Future work not in v1, roughly ordered by priority within each section.

## Near-term polish

- ~~**Themes.**~~ Done (2026-09-17): added as part of the libadwaita migration below - the Settings dialog's new "Appearance" group has a `ComboRow` (System/Light/Dark), persisted as `ui::settings::Settings::theme` (upgraded `settings.json` from a bare fader-max float to a small object; still reads the old bare-float format for anyone with an existing file) and applied via `adw::StyleManager::default().set_color_scheme()`. Defaults to Light per the earlier "brighter" ask.
- ~~**Fader UX polish.**~~ Done (2026-09-17), from direct user requests rather than a pre-existing backlog entry: the volume readout under each fader is now an editable `Entry` (type a number, Enter or click away to apply - parsed/clamped by the new `strip::parse_percent`, unit-tested), double-clicking either the master fader or the balance slider resets it (100%/center respectively, via a capture-phase `GestureClick` claiming the second press so the Scale's own click-to-jump handling doesn't fight it), and the balance slider now shows the per-channel percentages it implies (e.g. "L 100% · R 60%", `set_balance_label`). Click-tested live against real hardware (Scarlett Solo headphones out): typed values apply and round-trip through the over-amplification red-error styling correctly, both double-click resets work cleanly, balance label live-updates - reset the hardware back to 100%/centered afterward.
- ~~**Settings.** Settings menu for changing faders max, resetting default settings.~~ Done (2026-09-16): a gear button in the header opens a popover with a "Fader max %" spin button (persisted to `~/.config/sgithi-audio-meter/settings.json` via the new `ui::settings::Settings`, same pattern as `ui::overrides`) and a "Reset to default" button. Changing it applies live to every strip on both mixer pages (`DevicesPage`/`ApplicationsPage::apply_fader_max`), signal-blocked in `Strip::set_fader_max` the same way `Strip::update()` is, since lowering the ceiling below a strip's current displayed value clamps it and must not re-emit `value-changed` as a real `SetVolume`. Click-tested with the same isolated-instance `xdotool` approach as the popover/patchbay pass above.
- ~~**Higher over-amplification ceiling.** Faders already go to 150%...~~ Superseded by **Settings** above - the ceiling is now a user-configurable, persisted setting instead of a hardcoded constant.
- ~~**Rename/category popover and patchbay hide/show menu are code-reviewed but not click-tested.**~~ Done (2026-09-16): click-tested with `xdotool` against an isolated debug build (own `dbus-run-session` + a scratch `XDG_CONFIG_HOME`, never the user's real running instance or its config files, after an earlier attempt accidentally activated the real instance via GApplication's D-Bus single-instance handoff and had to be relaunched). Confirmed working: entry pre-fill/typing, category dropdown, Save (persists, renames, and re-columns the strip), and Reset to default (reverts both); patchbay right-click Hide (removes the node, persists) and right-click-empty-canvas Show (lists and restores a hidden node by name). Two things this pass surfaced that are still open:
  - **Intermittent Devices-page layout glitch after a category-change resync.** After a node's category override changes (moving its `Strip` between the Microphones/Other Inputs `FlowBox`es in `ui/devices/page.rs`), the destination section's heading `Label` and/or its strips' full height occasionally fail to render until the window is resized - didn't reproduce every time (2 of 3 attempts), and any manual window resize fixes it, so it looks like a stale GTK size-allocation cache rather than lost data. Not chased further this pass since the cause wasn't pinned down and CLAUDE.md calls for root-cause fixes, not guesses.
  - ~~**Patchbay nodes overlap heavily with no saved layout**~~ - root-caused and fixed under "Better auto-layout" below.
- ~~**Better styling.**~~ Done (2026-09-17): superseded by **libadwaita styling** below, once the user unblocked it by installing `libadwaita-1-dev` and gave concrete direction ("brighter and smoother").

## Medium-term features

- **Virtual audio cables.** Voicemeeter's signature feature — creating/destroying PipeWire `null-sink` "virtual cable" devices on demand from the UI. Explicitly out of scope for v1 (confirmed with the user during planning); v1 only controls/routes what already exists. Would need module load/unload management and probably persistence across restarts.
- ~~**Patchbay pan/zoom.**~~ Done (2026-09-16): panning was already free via the `ScrolledWindow`'s own scrollbars/scroll wheel; added Ctrl+scroll-to-zoom (`EventControllerScroll`, 0.25x-3x, `PatchbayPage::zoom`) instead of a `GestureZoom` pinch gesture, since this is a desktop mouse-driven app. `render::draw` scales the whole `cairo::Context` by the zoom factor, so every hit-testing/drag coordinate coming from a `DrawingArea` input event (always real widget pixels) has to be divided by it to land back in `CanvasModel`'s logical space - see the `PatchbayPage::zoom` doc comment for the full list of call sites that needed it (drag-begin/update/end, right-click hit-testing, link-hover). Click-tested in an isolated instance: zoom in/out, node drag while zoomed, and confirmed plain (non-Ctrl) scroll still pans normally.
- ~~**Better auto-layout.**~~ Fixed (2026-09-16): the actual bug behind the overlap confirmed in the near-term click-testing pass above wasn't the column heuristic itself, but `CanvasModel::sync()`'s per-column placement cursor being a local `[MARGIN; 3]` re-zeroed on every call - harmless if the whole graph synced once, but PipeWire discovers nodes one at a time, so in practice every node after a column's first landed at that column's starting y, stacking exactly on top of each other. Moved the cursor to a persistent `CanvasModel::next_column_y` field; verified with a regression test (`incremental_sync_does_not_stack_nodes_in_the_same_column`, added alongside a `batched_sync_matches_incremental_sync` sanity check) and visually in an isolated instance. The column heuristic itself is still just a first guess and may still want revisiting for large graphs.
- ~~**libadwaita styling.**~~ Done (2026-09-17), unblocked once the user installed `libadwaita-1-dev`. `src/ui/app.rs` now uses `adw::Application`/`ApplicationWindow`/`ToolbarView`/`HeaderBar`/`ViewStack`/`ViewSwitcher` (the crate is aliased `adw` in `Cargo.toml` the same way `gtk4` is aliased `gtk`, since its actual package name is `libadwaita`; needed the `v1_7`+`gtk_v4_10` feature flags for `PreferencesDialog`/`SpinRow`, which have no default features at all). `adw::StyleManager::default().set_color_scheme(PreferLight)` for the "brighter" ask. `ViewStack::add_titled_with_icon` needs a real icon per tab or `ViewSwitcher` shows a broken-image glyph instead - learned this by testing (`audio-card-symbolic`, `applications-multimedia-symbolic`, `network-workgroup-symbolic`; note the intuitive `multimedia-volume-control-symbolic` didn't render in this environment despite existing in the Adwaita theme's `symbolic/legacy` folder, while the exact same name without `-symbolic` works fine for the tray icon - a `legacy` icon quirk, not chased further). The Settings popover became an `AdwPreferencesDialog` with grouped rows (`PreferencesPage`/`PreferencesGroup`/`SpinRow`/`ActionRow`) instead of a flat `Popover`, per the user's "sub menus for places with lots of options" ask - same fader-max/reset behavior, just presented as an in-window sheet with room to grow. Click-tested live in an isolated instance: all three tabs render with correct icons, the Settings sheet opens/closes and its spin row and reset row both work identically to the old popover.
- ~~**Per-channel / balance control.**~~ Done (2026-09-16): added a small horizontal balance slider (-1.0 full left .. 1.0 full right, pavucontrol's convention - the louder channel stays at the master fader's value, the other is attenuated) below the volume label, shown only for a 2-channel node and hidden otherwise (`Strip::update` re-checks this every sync, since a node's channel count often isn't known yet at `Strip::new` time - before its first `Props`/Route volume param arrives). Balance is tracked separately from the master fader (`Strip::balance`) so moving either one doesn't clobber the other, and reconciled back from real per-channel volumes on every `update()` in case something external changes them. `balance_to_volumes`/`balance_from_volumes` are unit-tested, including a round-trip test. Not attempted for >2 channels - no real device on hand to design that UI against, and pavucontrol itself only exposes balance for stereo too. Click-tested live: balance persists across independent fader moves, and un-does cleanly back to centered/equal channels.

## Longer-term / stretch

- **Automated tests.** Grown organically this session alongside other work rather than as its own pass: `ui::volume` (linear/perceptual conversion), `model::Graph::apply`, `pw::node_props`/`pw::device_route` (pod (de)serialization), `ui::patchbay::canvas_model` (auto-layout placement), and `ui::strip`'s balance math all have unit tests now (`cargo test`, 22 as of 2026-09-16). Still untested: `pw::registry`/`pw::device_profile`/`pw::metadata`/`pw::peak` (all tightly PipeWire-coupled - would need a mock or integration harness, not a quick add) and the GTK-side page/render code (same problem, GTK-coupled).
- ~~**CI.**~~ Done (2026-09-16): added `.github/workflows/ci.yml` (build + `cargo test` on every push/PR to `main`), reusing the same apt install line as the existing `release.yml` (which only builds a release binary on a `v*` tag push - a separate, already-working workflow that predates this session). Not pushed yet, so not actually confirmed green on GitHub's runners - only locally, where `cargo check --all-targets` and `cargo test` both pass.
- ~~**Reconnect handling.**~~ Partially done (2026-09-16): a full live reconnect isn't attempted - the `Command` channel's `Receiver` is consumed by `Receiver::attach`, permanently tied to the PipeWire thread's one `MainLoopRc`, so reconnecting would mean building a new channel and re-pointing every `Command` sender already cloned into every page and `Strip`. Instead, `pw::thread` now listens for a core-level error (`id == PW_ID_CORE`, e.g. a daemon restart) and sends a new `Event::Disconnected` before exiting, which `ui::app` uses to update the window title to say so plainly. Live-verified (with the user's permission) by restarting the real `pipewire`/`wireplumber` user services against an isolated instance: the pipewire thread exits, the window survives showing an accurate empty state (every node genuinely was removed via normal `global_remove` events as the daemon tore down) plus the "disconnected - restart to reconnect" title, and the app process itself stays alive rather than hanging or crashing. A true hot-reconnect (rebuilding the whole `Command`/`Sender` plumbing) is still open if that's ever worth the churn.
- ~~**Search/filter.**~~ Done (2026-09-16): both `DevicesPage` and `ApplicationsPage` got a `gtk::SearchEntry` above their columns, filtering strips by a lowercased substring match against `Strip::display_name()`. Applied both live (as the entry's text changes) and at the end of every `sync()` (so a rename or graph change can't leave a strip's visibility stale) via a shared `apply_filter` free function - required making each page's `strips` map `Rc`-wrapped so the search entry's handler, wired up in `new()` before any `Rc<Self>` exists, can share it directly. Click-tested live: typing narrows the Devices page down to the matching strip, clearing brings everything back.
- ~~**Tray icon / background mode.**~~ Done (2026-09-16): GTK4 dropped the old `StatusIcon` API, so this uses `ksni` (a pure-Rust StatusNotifierItem implementation, `blocking` + `async-io` features - `async-io` instead of `tokio` since nothing else in this app uses a tokio runtime) on its own thread, matching `pw::thread`'s pattern: tray/menu callbacks fire there and are turned into a new `tray::TrayEvent` sent to the GTK thread over an `async_channel`. Closing the main window now hides it instead of quitting (confirmed with the user first, since it changes existing behavior) - a `really_quit` flag distinguishes that from an actual quit via the tray's "Quit" item, which sets the flag and then closes the window for real. A failed tray registration (no `StatusNotifierWatcher`, e.g. this project's own isolated-`dbus-run-session` test instances) is logged and otherwise non-fatal - the window still works normally, just without a tray icon. Verified two ways: the isolated-instance approach for the close-to-tray/hide logic and the graceful-failure path, then (with the user's permission, since this is the first time connecting a test instance to the *real* session bus rather than an isolated one - checked first that the real app wasn't running) against the real desktop session, confirming via `busctl` that the item actually registers with the real `org.kde.StatusNotifierWatcher`, and that simulated `Activate`/menu `Event` D-Bus calls correctly toggle the window and quit the app. The icon didn't visibly render in this environment's particular panel despite registering correctly - likely a systray-plugin/icon-theme quirk specific to this multi-monitor/remote setup, not something pinned down further this pass.

## Standing lessons (not features, but worth keeping visible)

- **Never call `enum_params` with an unbounded count (`u32::MAX`)** against a live PipeWire/WirePlumber session. Early hardware-volume (`Device`/`Route`) work did exactly this and left the user's real `wireplumber` process stuck at 100% CPU system-wide, requiring a service restart to recover. The current code bounds these calls (8 for Node `Props`, 32 for Device `Route`); keep that pattern for any new param-enumeration code, and be cautious re-testing PipeWire-touching changes repeatedly against a live session — a CPU watchdog on `wireplumber` alongside any test launch is cheap insurance.
- **A registry global's initial `props` (from the `global`/`global_remove` listener) is not the same set as a bound object's full `info().props()`.** `card.profile.device` - needed to link a hardware Node to its `Device` Route (see `device_route.rs`) - is present in a Node's `info` event but *absent* from its registry `global` event props, even though `device.id` (the other half of that link) is present in both. This caused a real, silent bug: `route_link` was always `None`, so every hardware node fell back to reading its own (non-authoritative, sometimes stale) `Props`, and looked approximately-but-not-exactly right on screen - close enough to pass a casual visual check, wrong when cross-checked against `wpctl`. Any future code reading a property from a `GlobalObject`'s `props` should verify that property is actually present there (`pw-dump`'s output is `info().props()`-shaped and does *not* prove a key is in the registry global props) rather than assuming; if it isn't, get it from an `.info()` listener on the bound object instead, as `registry.rs`'s Node arm now does.
- **Programmatically updating a GTK widget that has a `connect_*` signal handler attached can re-emit that signal**, which for this app's fader/mute controls means silently sending a real `SetVolume`/`SetMute` command for a change the user never made - not just a display bug, a write-back into the user's real audio state. `Strip::update()` (called on *every* graph event, not just ones relevant to that strip) now blocks the relevant `SignalHandlerId` around each programmatic `set_value`/`set_active` call; any new widget in this app that both displays incoming state and accepts user input needs the same treatment.
- **A `pipewire::stream::Stream` you create is itself a regular registry `Node` - if your own registry listener doesn't know to ignore it, it will discover its own metering/probe streams as if they were application audio.** Building peak metering (`pw::peak`) this way created a real feedback loop live: each `Strip`'s metering stream registered as a `Stream/Input/Audio` node, `ApplicationsPage` gave *that* its own `Strip`, which started metering *it*, exponentially - caught within seconds via dozens of phantom "app streams" and the app process (not `wireplumber`) pegging a CPU core. Also: the `name` argument to `StreamRc::new`/`pw_stream_new` does **not** become the resulting node's `node.name` (that defaults to the whole process's name) - you must set `PW_KEY_NODE_NAME` explicitly in the stream's properties if `registry.rs` needs to filter your stream out by name, as `pw::peak::STREAM_NAME_PREFIX` now does.
- **Routing a high-frequency PipeWire event through the same full-page `sync()` used for occasional ones (volume/mute/rename) doesn't scale.** `sync()` reconciles every strip's placement, name, volume and mute state - fine for a user-driven change, but `Event::PeakLevel` alone arrives at ~30Hz *per watched node*, and even with only half a dozen strips on screen, running a full `sync()` for every tick pegged a CPU core (confirmed: dropped from ~130% to ~15% total after fixing). A frequent, narrowly-scoped event should update just the one widget it affects (see `ui::app`'s event loop special-casing `PeakLevel` to `Strip::set_peak`/`*Page::update_peak` instead of `sync()`) rather than reusing the general reconcile path.
- **A GTK widget attached with `set_parent()` (rather than added to a container via `append`/`add`) isn't detached just because its parent gets torn down or removed from *its* own parent.** `Strip`'s rename/category popover is `set_parent()`ed to the pencil button, so when a strip's nodes get removed and recreated (confirmed live: switching the Scarlett Solo's PipeWire profile, which drops/recreates all its nodes), dropping the old `Strip` finalized the pencil button while the popover was still attached - logged by GTK as "Finalizing GtkButton... but it still has children left" for every torn-down strip. Fixed by having `Strip`'s `Drop` explicitly call `self.edit_popover.unparent()`. Any future widget built with `set_parent()` instead of a container method needs the same explicit unparent in whatever teardown path drops it.
